{
  "name": "clip-forge",
  "version": "1.0.0",
  "private": true,
  "description": "In-browser MP4/audio trimmer and joiner (ffmpeg.wasm) for GitHub Pages.",
  "type": "module",
  "scripts": {
    "validate": "node --check js/app.js && node --check coi-serviceworker.js && node scripts/validate.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test": "npm run validate"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
